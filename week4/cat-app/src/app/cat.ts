
/*
    What are Interfaces in TypeScript?

    Think of the role of Classes in Java. We are defineing a custom Type of Object
    We use interfaces in TS to define the TYPES of properties that a particular object can have
    We use interfaces to define the "shap" of an object
*/

// We are defining a custom type
export interface Cat {
    id: number;
    name: string;
}